pen_lom_missions = {
	slot = 1
	generic = no
	ai = yes
	has_country_shield = yes
	
	potential = {
		OR = {
			tag = LOM
			tag = PNE
		}
	}
	
	# Mission to form Pen Lom
	
	green_viper_union = {
		icon = mission_empire
		generic = no
		position = 1
		completed_by = 1445.1.1
		
		provinces_to_highlight = {
			owned_by = LOM
			owned_by = PNE
			NOT = {
				country_or_non_sovereign_subject_holds = ROOT
			}
		}
		trigger = {
			NOT = {
				exists = PEL
			}
			custom_trigger_tooltip = {
				tooltip = green_viper_union.tooltip
				if = {	
					tag = LOM
					
					AND = {
						alliance_with = PNE
						marriage_with = PNE
						
						calc_true_if = {
							PNE = {
								has_opinion = {
									who = ROOT
									value = 150
								}
							}
						}
					}
				}
				if = {	
					tag = PNE
					
					AND = {
						alliance_with = LOM
						marriage_with = LOM
						
						calc_true_if = {
							LOM = {
								has_opinion = {
									who = ROOT
									value = 150
								}
							}
						}
					}
				}
			}
		}
		effect = {
			if = {
				tag = LOM
				inherit = PNE
			}
			if = {
				tag = PNE
				inherit = LOM
			}
			change_tag = PEL
			swap_non_generic_missions = yes
			if = {
				limit = {
					NOT = {
						government = monarchy
					}
					is_revolutionary = no
				}
				change_government_to_monarchy = yes
			}
			if = {
				limit = {
					NOT = {
						government_rank = 2
					}
				}
				set_government_rank = 2
			}
			add_prestige = 20
		}
	}
}